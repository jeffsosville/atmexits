# Placeholder — ATMExits does not run scrapers
# Sellers self-list via the intake wizard
name: placeholder
on: workflow_dispatch
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No automated scrapers for ATMExits"
